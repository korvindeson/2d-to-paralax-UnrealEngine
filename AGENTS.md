# FaceParallax — Agent Guide

Product docs: README.md

## Layout

- **Root `G:\tailedstories\paralax` is canonical** — the 20 `.h`/`.cpp` sources at the root are the single source of truth. `Tests\run_tests.ps1` syncs them into `SAMPLES\MyProject\Plugins\FaceParallax\Source\` before every UE build; never edit plugin copies directly.
- **Plugin**: `SAMPLES\MyProject\Plugins\FaceParallax\` — two modules:
  - `FaceParallax` (Runtime, `PostDefault`) — component, preset, preview actor, depth debug visualizer, types.
  - `FaceParallaxEditor` (Editor, `PostDefault`) — editor subsystem, editor widget, deploy pipeline. The widget is split across `FaceParallaxEditorWidget.cpp` (core API), `FaceParallaxEditorWidgetUI.cpp` (`RebuildWidget` — slim skeleton wiring panel builders + head/marker code), `FaceParallaxEditorWidgetInteractions.cpp`, `FaceParallaxEditorWidgetPanels.cpp` (panel construction builders `BuildPanel*` + `MakeSectionBox` + refresh/rebuild functions), plus `FaceParallaxEditorWidgetShared.h` (anonymous-namespace helpers + `SFaceLayerGizmo` used by all widget TUs).
- **Phase H design contract**: `FaceParallaxLayoutSpec.h` (root, pure C++17, synced into the editor module Private dir) is the layout manifest + P1–P24 validator for the widget tree. `RebuildWidget` self-checks it; `Tests\ParallaxMathTests.cpp::TestPhaseHUIDesign` asserts zero violations, the exact 5-page context-panel viewport set (pages 621×800 clipped `bNoVScroll` stacks — the context panel is FIXED at `ContextPanelWidth` = `MainRowWidth − CenterColumnMinWidth` = 1089 − 468 = 621 (the old `RailWidth` 273 + `PropsWidth` 340 + `PropsRightGap` 8), derived from the edge-schematic section's empty space, with NO internal splitter: the rail/canvas resizer, the rail-width spinbox, and the props-pane carousel are all gone; resizing only happens at the very outside of the widget; P22: context pages never scroll horizontally, every non-flex child must fit the `FixedW` margin box, `bAspectRatio` nodes must resolve to `FaceAspectRatio` (P23, `CN-Preview` 450×450), and P24 NoTerminalOverlap keeps every MainRow column + center-column row inside the `MainRowHeight` band so nothing slides under the terminal output window; the Status Detail matrix is mirrored as a paged carousel `SD-Carousel`/`SD-CarouselNav` because the real grid is unbounded — its last layer row ("Hair") pages inside a fixed viewport and the mirror test asserts the unpaged table fires P17 while the paged one fits), mirrored design constants (P14: context panel reaches the window edge while the center column keeps `CenterColumnMinWidth`; P15: context pages are no-scroll stacks), negative controls for every principle, and section auto-stack (manifest sections are never Flex in a clipped viewport and sibling rects never overlap). The Python validator additionally flags any real-widget section slot (`->AddSlot()` feeding a `MakeSectionBox`) that lacks `.AutoHeight()`/`.FillHeight()` — the "View Override paints over Scale Y" defect class. Pool order is child-first (args evaluate eagerly) — never index a child via `parent + N` arithmetic; use `Children[...]` lookups (`Bod(sec) = Children[1]`).

## Phase status (all integrated + verified)

Every phase is landed in the product code AND covered by a live suite in `Tests\ParallaxMathTests.cpp` (all green: 2584/2584 + validator + UE build + headless probe):

- **Phase A** (workspace rails, view strip dots, drag-drop thumbs, dev tools relocation) — `TestPhaseEFMirrors` + widget code; README §Editor.
- **Phase B** (labeled 5-tab rail regroup, gizmo/alignment, onion-skin, sync) — `TestPhaseBAlignmentMirrors`. Superseded structurally by W1 (the 5 rails became 4 context pages + a Developer drawer), but the Phase B gizmo/alignment/onion-skin/sync features themselves remain.
- **Phase C** (unified inspect mode, folder wizard, sync drift, selection outline, canvas selection) — `TestPhaseCMirrors` + `TestPhaseCIntegration`.
- **Phase D** (display modes, depth debug, edge overlay/histogram, hull review, grouped sync) — `TestPhaseDMirrors` + `TestPhaseDSyncIntegration`.
- **Phase E** (viseme grid, nested outliner, pin manager, param bindings, layer thumbs/badges, undo shortcuts) — `TestPhaseEFMirrors` + `TestPhaseLayerBadgeMirrors` + `TestPhasePinMgrMirrors`.
- **Phase F** (problems panel, ring-buffer undo, shared drag-drop pipeline `AssignImageDropToSlot`/`AssignImageDropToBlinkFrame` replacing legacy pick buttons) — `TestPhaseEFMirrors` + `TestUndoStack*`.
- **Phase G** (outline-depth bake quantization; the old rail `RL-Advanced` → `RL-Diagnostics` grouping, now re-homed into the Developer drawer) — `TestPhaseGWidgetMirrors` + `TestPhaseHUIDesign`.
- **Phase H** (layout design contract, P1–P24 validator, self-check in `RebuildWidget`) — `TestPhaseHUIDesign`.
- **Phase I** (fit-first P17 / carousel P18 / reserve P19 UI procedures) — `TestPhaseIUITesting`.
- **Phase I edge map** (group-colored part edges: `FPEdgeGroup` eyes/mouth/hair/surface, `FPEdgeLuminanceForClass` front-lighter-than-back, hair detail levels `FPHairLevelForTag` + `FPEdgeMapShows` toggle; Canvas Options "Edge map"/"Hair edges" in `BuildPanelCanvas`) — `TestEdgeMapMirrors`.
- **UX Phase 1** (interactive transform gizmo: `SFaceHotspotLayer` routes move=box-edge-ring/rotate=top-handle/scale=bottom-right-corner through the static pure contract `GizmoHitTest`/`GizmoApplyDrag`; the gizmo leaf stays paint-only; box interior stays a part-click miss so P1 one-map is untouched; handle cursors; drag snapshots at mouse-down so it can't drift) — `TestPhase1GizmoInteractiveMirrors`. Supersedes the Phase 0 "gizmo is decorative" defect (B1).
- **UX Phase 2** (direct art import: canvas drag-drop accepts OS image files + Content Browser texture assets via `SFaceHotspotLayer` drag handlers + `DragHasImagePayload`, drop target resolves as part-under-cursor-wins-else-selected-layer via the static pure contract `CanvasDropTargetLayer`/`HandleCanvasDrop`, and routes through the shared `AssignImageDropToSlot` channel-suffix pipeline; part click on an artless layer opens the native OS picker `OpenImportArtDialog`, with "Import Folder..." wizard retained as the bulk path; a live drop ring paints while a valid drag hovers) — `TestPhase2DirectImportMirrors`. Supersedes the legacy "pick button opens a folder" import defect.
- **Numbered remediation phases** (0 click routing, 1–5 pins/hotspots/previews, 4b accessibility, P3 assign page, P21 pinned-actions rule) — `TestHotspot*`, `TestPinDriftMirror`, `TestPhase4Mirrors`, `TestPrimaryLayerPin`, `TestPinRotation`, `TestAccessibilityMirrors`, `TestPinnedActionsRule`, `TestPreviewModesMirror`, `TestPhaseP3Mirrors`.
- **Remediation batch 1** (W2 hover-label + schematic cycle `FPSchematicPartStackCount`/`FPSchematicPartCycleAt` + `FPHoverPartLabel`/`FPSchematicCycleIndex` + hotspot repeat-click cycling; W3 native picker is the primary import path — pinned "Import Art..." uses `OpenImportArtDialog`, folder wizard stays bulk-only; W8 status badge `FPStatusSummary` + `TextStatusBadge`/`RefreshStatusBadge` on the view strip + flash-button Reset/Dup) — `TestBatch1StackCycle`/`TestBatch1HoverLabel`/`TestBatch1StatusBadge`.
- **Remediation batch 2** (W4 Sync + Align op selector two-defaults + "more..." disclosure `SyncOpDefaultOps`/`SyncOpMoreOps`/`SyncOpIsDefault`/`SyncOpIsMore` + per-state view-strip drift dots `FPViewStripDrift`/`FSyncDrift` + `RefreshViewStripDrift`; W5 canvas pin-placement marker + `<Layer> pin` label painted by `SFaceSchematicLayer` when `IsPendingPinPlacement`; W6 canvas transform readout `FPTransformReadout` painted top-left of the gizmo; W7 the four display-mode rail Config checks retired — the 5-way segmented mode row is the sole display control, the Developer drawer's Config keeps only Blinking/Swoosh/Nested Art/Params, disclosure "K of 4 on") — `TestBatch2SyncOpSelector`/`TestBatch2ViewStripDrift`/`TestBatch2TransformReadout`/`TestBatch2ModeRow`.
- **Remediation batch 3 (W1)** (structural collapse: the TopTabs + 5-rail switcher + separate 340px props pane are replaced by ONE 621px context panel switched by a 5-button task tab row `CT-TabRow`/`SetActivePageIndex` — pages Assign / Transform & Sync / Expression,Blink & Viseme / Preview & Debug + the closed-by-default Developer drawer; MainRow = `[Center | CP-ContextPanel]`, `ContextPanelWidth` = `MainRowWidth − CenterColumnMinWidth` = 621; the old `RAIL-Switcher`/`RL-*`/`PROPS`/`PR-Carousel`/`PR-CarouselNav`/`PR-Scroll` machinery is retired; section registry re-based as `PageSectionTitles`/`FindPageSectionByTitle`; kept-legacy `RailWidthPx`/`RailWidthSpin`/`RailResizer`/`GetRailWidthPx`/`SetRailWidthLive`/`ApplyRailWidthDelta` — `SetRailWidthLive` now writes `ContextWidthBox`) — `TestPhaseHUIDesign` (518-node manifest, zero violations), `TestAccessibilityMirrors` (page registry, remapped out-indexes), `TestPhaseIUITesting` (page-name arrays).
- **Phase 3** (ONE Copy/Sync panel: every sync/copy path consolidated onto the Transform & Sync page's **Sync + Align** section — op selector Transform/Textures/Both, always-visible 2×5 destination grid with "This" disabled, Apply to picked/All, Copy-from combo, Fill missing, Link broadcast + drift indicator; the state-strip pick mode `bStatePickMode`, the `BuildApplyToViewsContent` popups, the Assign page Quick Actions + Cross-View Transform sections, and Assign Ops "Apply views" anchor are all gone; view tabs always switch views; per-view override row moved onto the Transform section) — `TestPhaseHUIDesign` (518-node manifest, zero violations), `TestAccessibilityMirrors` (page-0 registry now 6 sections, 'quick' no longer matches), plus always-visible-dest-grid / no-pick-mode mirror tests in `TestPhaseHUIDesign`. LayoutSpec `PageSectionTitles` page 0 = Selected Layer/Layers/Import/Outline -> Depth/Bulk Assign/Assign Ops.
- **Schematic redesign (strict anime-girl, Phase A)**: the 17 front-facing glyphs in `DefaultPartSchematics()` (and the mirrored 13 `DefaultHotspotRegions()` in `FaceParallaxLayoutSpec.h`) were re-authored to the strict anime rules — upper head a WIDE sphere tapering to a pointy V chin, eyes on the head's ABSOLUTE vertical center (y≈0.44, skull top 0.02 / chin 0.86) with a gap equal to one eye width, minuscule triangle nose halfway between the eye line and the chin (0.65), tiny shallow mouth much closer to the chin than the nose (0.74–0.82), ears spanning eye-top to nose-bottom, thin sweeping brows wider than the eyes arching over the bangs, jagged tapered hair wedges (bangs over brows, side locks framing the jaw, big back mass). The hair stays an ANNULUS (outer mass + face cutout), the Mouth keeps its open-hole + Teeth ring, and the head contour stays wide at the eye line, so every probe, region-parity, stack-depth (Teeth+Mouth+Head=3, Mouth+Head=2, Bangs+Head=2), boundary-inclusive and cycle invariant in `TestSchematicParts`/`TestBatch1StackCycle`/`TestHotspotRegions` holds exactly — the probe/region/stack coordinates were moved in the same change with identical test intent (all green).
- **Construction-detail pass (all 17 authored, final geometry)**: every one of the 17 parts resolves from the authored pose table `FPSchematicAuthoredPoseTable()` (7 rings each — the `FPSchematicBuildFeaturePoseSets` formula stays only as a safety net for non-table parts), and the front glyphs carry the final guide-rule details: the eye glyphs insert a 3-point iris arc that tucks under the upper lash (EyeL/EyeR go 10→13 points in EVERY ring, inserted between the two upper-lash wedge points at 25/50/75% with a +0.012/+0.020/+0.016 dip — the W/H extremes and the 0.104/0.1372 aspect pin are untouched, and EyeR stays the exact mirror of EyeL to 1e-9), the Mouth is the narrow shallow lip (W 0.16, band center 0.779 inside the [0.776,0.797] mouth-band pin; P45 = scale(0.80,0.85)+shift(+0.015,0), P90 = scale(0.20,0.50)+shift(+0.06,0), P180 = scale(0.80,0.80)+shift(0,−0.14)), the Bangs insert the 3-point **ahoge cowlick** breaking the centerline mirror (24→27 points in every ring, a 0.025-tall upward spike just right of the center part at 10/40/68% of the wedge), and the Head P90 profile pose recedes the chin vertex to (0.30,0.84) toward the face line. The regeneration is scripted (temp `remediate.py` in the Temp dir, parse-anchored on the flat `DefaultPartSchematics` block and the table block, bottom-up edit application, full pin validator incl. the 100%-back-gate on the 13 cards and the 41% aggregate gate on the silhouettes) — re-running it is idempotent; it must keep ALL pins green: eye mirror slots {0,2,3,4,5,6} at 1e-9, P45 width ratios at 1e-6, mouth band center, bangs hem clears the brow top (`maxY(Bangs) < minY(BrowL)`), jaw origins HEAD[2]/HEAD[10] at (0.164,0.356)/(0.836,0.356), 5-part segments all exactly one eye width, stack depths (0.50,0.79)=3/(0.50,0.76)=2/(0.50,0.06)=2, and every hotspot/schematic probe.
- **Smooth-turn orientation (Phase B rework, canonical spec)**: `FPOrientationOutline` is now a BILLBOARD model — real 2D art is a stack of flat cards, so every part always faces the camera (the surface normal never turns edge-on / never paper-thins) and the turn is faked by SLIDING five Z-depth planes against each other (`FPZDepth`: 1 Nose/Front Bangs, 2 Near Eyelash/Iris/Eyebrow/Mouth, 3 Face Base (cranium+jaw) + the far-side eye, 4 Near Ear/Side Hair, 5 Neck/Back Hair) via camera-translation parallax (`FPYawSlidePeak`/`FPYawSlideAt`, closest Z slides furthest, the Z-5 backdrop NEVER slides; yaw slide-then-flip at the exact 45/90/135/180 state centers, `FPRampEval` smoothstep, left-half mirror), the far-side pair compresses to zero by yaw 90 and STAYS folded through the back, features fade 1.0→0 across 112.5–135, silhouettes stay full width at 0 AND 180; pitch is the encroach/counter contract (`FPPitchRoleForPart`/`FPPitchMagnitude`/`FPOrientationVerticalShift`: features + hair ENCROACH down at Top / up at Bottom, ears + V-chin COUNTER-translate, face base near-static) plus vertical squash `FPOrientationPitchScale`. The old hybrid helpers (`FPOrientationShift`/`FPProfileBumpAt`/`FPMouthOffsetAt`/the `FPDepthClass` `FPOrientationVerticalShift`) are removed. — `TestPhase2Orientation` (rewritten to pin the billboard contract: Z-depth ordering, backdrop-never-slides, far-edge slide direction, far-fold-through-back, pitch encroach/counter magnitudes, nose-darts-to-the-far-edge, mirror + containment sweep).
- **Authored silhouette poses (placeholder-completion roadmap, all landed)**: the silhouette parts (Head/Bangs/Hair/BackHair) no longer rely on the squish/slide formula — `FPSchematicAuthoredPoseTable()` is a sentinel-terminated table of authored `FPSchematicPoseSet`s (P0/P45/P90/P135/P180/PTop/PBottom key poses per part, ring-ordered like the front glyph), `FPSchematicAuthoredPoses(Name)` returns nullptr for any part outside the table (every anatomical feature falls back to the formula), and `FPOrientationOutline` branches first to `FPOrientationAuthoredMorph` (per-vertex `FPSchematicYawMorph` smoothstep between the bracketing yaw anchors, pitch blend weighted by yaw-fade, negative-yaw mirror X→1−X) for authored parts while non-authored parts keep the formula verbatim. The 180° BackHair pose TRAILS the profile skull (defect fix: the formula's Z-5 backdrop never slides). — `TestAuthoredOrientation`.
- **Phase 2 authored feature-card matrix (guide Parts IV/V, all landed)**: the canonical 17 set now includes the 13 anatomical cards (eye/brow/cheek/ear pairs + nose/mouth/teeth/chin/neck) — `FPSchematicFeatureRingAt` derives each per-zone ring from the part's front glyph by pure mirror-consistent transforms (centroid scale + pure-Y shift; paired parts never translate X — it remains ONLY as the fallback for non-table parts); ALL 13 cards' P45/P90 slots are HAND-AUTHORED in `FPSchematicAuthoredPoseTable` (per-segment compression, not a uniform scale — the outer/profile-side edge compresses more than the nose-side edge, verified per-card) and re-pinned against formula re-bakes (every ring must differ from `FPSchematicFeatureRingAt` output): **P45** = the near/far role split — near member `Eye_Near_3Q` ~0.84 vs far member `Eye_Far_Narrow` ~0.50, `Brow_Far_3Q` ~0.60 vs near brow ~0.80, near ear ~0.91 vs far ear ~0.76 (EarL = canonical far card at +yaw), near cheek ~0.82 vs far cheek ~0.70, mouth `Mouth_3Q` = compressed-off-center 0.80 + ~0.02 shift, nose darts toward the turn side (CX ~0.53), teeth/chin follow the same dart+compress rules; **P90** = the profile sliver (eye 0.014 / brow 0.040 / cheek 0.018 / ear 0.016 wide — every card stays <0.08 wide, the near ear P90 runs TALLER than its front glyph; centerline cards drop into the contour as narrow placeholders), **P135** = ear back-fuzz (1.30/0.50), **P180** = folded card (0.80 scale + −0.14 pure-Y shift, clears the Phase 6 >10% back-change gate), **PTop/PBottom** = front glyph. The left-half states (5–7) resolve the PARTNER's ring mirrored (`FPSchematicPairPartner` + the swap inside `FPSchematicOutlineForState`), so the −45 view stays the exact horizontal mirror of +45 with the near card riding the left side (only the P45 slot is role-split; every other slot keeps slot-for-slot L==mirror(R)). Visibility runs BEFORE resolution: Top (8) drops ALL BridgeSafe cards (Part V.2; Head/Bangs/Hair/BackHair/Ears stay), profile 2/6 merges Nose/Mouth/Teeth into the contour, far-side pair members fold ≥90°, walk-behind 3/4/5 fades everything but the AnchorCritical ears. Bottom (9) is NOT a drop state — Part V.4's under-plane asset is carried by parallax, so `FPSchematicLayerVisibleInTag` only excludes state 8 and the Bottom ring equals the front glyph with no −90 corner in the blend space (Part VI). The Part IV 3/4 grid "compression" of the near column refers to POSITIONS (the compressed-off-center mouth and the darting nose), not card widths — the only width compression in the matrix is the far-narrow P45 cards and the P90 profile slivers. Delta/swoosh numbers re-pinned to the crown drop (Front→Top = 0.4, Top→Back = 0.3 angle-only, Top→Bottom = 0.5; Front→Top and Top→Bottom now swoosh). — `TestPhase2AuthoredFeatureMatrix` + re-pinned `TestPhase2Orientation`/`TestAuthoredOrientation`/`TestAnchorClass`/`TestPhase3Visibility`/`TestPhase4SilhouetteDelta`/`TestPhase7ArtSwap`. Synced into `SAMPLES\...\FaceParallaxSchematic.h` by `run_tests.ps1`.
- **Anchor classes (bridge-safe vs anchor-critical)**: `FPSchematicAnchorClass` + `FPSchematicAnchorClassForPart`/`FPSchematicAnchorClassForTag` — the 4 silhouettes + ears are AnchorCritical (the read-carrying cards), every feature is BridgeSafe, unknown/null/empty default BridgeSafe. — `TestAnchorClass`.
- **Per-state visibility + Z-order (hide, not just occlude)**: real 2D art cards cannot fold to a dot, so `FPSchematicLayerVisibleInState` (per-part: silhouette mass always renders, the far-side member of a pair hides at the profile, non-paired features hide in the walk-behind states BackRight/Back/BackLeft where |center yaw| ≥ 135) and its TAG-level runtime variant `FPSchematicLayerVisibleInTag` (the component spawns ONE quad per base-preset tag, so feature cards Eyes/Brows/Mouth/Nose/Cheeks hide only walk-behind, the silhouette+Ears cards stay in the read) drive `UFaceParallaxComponent::RefreshLayerVisibilityForState` from `ApplyCurrentStateTextures` — no art edge-peeks around the skull. `FPSchematicLayerOrderInState`/`FPSchematicLayerOrderInTag` give the per-state Z-order (hidden = −1). State indices mirror `EFaceAngleState` order; zone centers are the default multipliers (0/45/90/135/180/−135/−90/−45, Top/Bottom at yaw 0 pitch ±90). — `TestPhase3Visibility`.
- **Silhouette-delta crossfade / swoosh**: `FPSilhouetteDelta(From,To)` scores the structural gap (60% shape term = fraction of base-preset tags whose visibility flips + 40% wrap-aware angle term), `FPSchematicTransitionBlendRate` biases the crossfade speed up to 2.5×, and `FPSchematicShouldSwoosh` is the trigger extension — the runtime swoosh trigger (`UpdateStateMachine`) now also fires when `FPSchematicShouldSwoosh(PreviousState, CurrentState)`. NOTE (B.2): the non-swoosh blend tick NO LONGER multiplies `CrossfadeSpeed` by the blend-rate bias — the runtime crossfade is now the parameter-space window (below), so `FPSchematicTransitionBlendRate` remains a pure contract consumed only by the pins. — `TestPhase4SilhouetteDelta`.
- **Phase B.1 — per-zone rebased sine `RampOffset` (art_guide III.2/III.6 velocity + Local Delta Reset)**: the parallax slide became `offset(θ) = Peak × [sin(θ°) − sin(θ_a°)]` — the SAME global sine sampled-and-shifted down per zone instead of a generic smoothstep — with `θ = key + fraction × HZW` (fraction −1..+1 from the authored pose key, `SignedFraction`), `FPYawRule::RampOffset(SignFrac, DepthFactor, MaxOffset, SignedKeyDeg, HalfZoneWidthDeg)` in the schematic header, and the 5-arg call sites in `ComputeOffsetForState`/`UpdateMaterialParameters` (evaluated with the `YawSign * GetYawKeyForState(State)` signed anchor). Velocity ∝ cos(θ): fastest at the front pole, zero at the 90° profile, decelerating zone-over-zone, and the −π/2 boundary velocity is inherited exactly by the incoming zone (a Peak-value change at a boundary ramps across the crossfade window per III.6 Trajectory Matching). — re-pinned `TestYawRule` (5·sin45 = 3.5355339059327378 in / 3.5355 out, −1.7679 Ears, clamps 5/−5, half-depth 2.5, Head 0, Hair 1.5, Eyes 3) + a cos-velocity finite-difference pin (KeySlope ≈ 7.854, BndSlope ≈ 0).
- **B.2 — parameter-space crossfade (art_guide III.6 Parameter-Space Crossfade Width / IV.0)**: the frame-count `FInterpTo(DeltaTime, CrossfadeSpeed)` blend is replaced by a pure function of the rotation parameter — `FPSchematicCrossfadeAlpha(ParamDeg, BoundaryDeg, DirectionSign)` (with `FPSchematicCrossfadeHalfWindowDeg` = 0.75 and `FPSchematicCrossfadeSchmittDeg` = 1.5) returns α = 0.5 at the hysteresis-adjusted trigger (split Schmitt: boundary + Sign·1.5°) and ramps linearly 0→1 across ±0.75°, wrapping across ±180 so a Back pair crossing keeps measuring the signed sweep; the `UpdateStateMachine` blend tick feeds it the pair's signed boundary (`GetBoundaryBetweenStates`) + the direction sign (+1 = increasing parameter; the ±180 Back↔BackLeft pair and the pitch axis use explicit rules) + the live yaw/pitch, and completion happens purely from position — a fast drag fades over the same angular sweep as a slow turn and retraces without lingering. `bUseContinuousBlending` now actually gates the fade (off = instant hard swap, on = parameter-space window); `BlendWindowWidth` re-scoped to the half-width in degrees (default 0.75); `CrossfadeSpeed` retained for Blueprint compatibility but documented as a no-op. — `TestParameterSpaceCrossfade` (28 pins: trigger centers, window edges, clamps, linear slope, left-half mirror, Schmitt offset, monotonic sweep, Back-wrap, Back↔BackLeft pair, direction-sign normalization, speed-independence) + re-pinned `CrossfadeWindowStart` harness (BlendW 5.0 → 0.75).
- **B.3 — directional Schmitt state-flip commit (art_guide IV.0)**: the state flip is NOT a frame-count debounce (a frame window is speed-dependent, the same defect B.2 removed from the crossfade). `FPSchematicSchmittTriggerAt(BoundaryDeg, DirectionSign)` (forward at `Boundary + Sign·1.5°`, reverse at `Boundary − Sign·1.5°`, reusing `FPSchematicCrossfadeSchmittDeg` = 1.5) + `FPSchematicSchmittCrossed(ParamDeg, BoundaryDeg, DirectionSign)` (wrap-aware signed sweep across ±180 so the Back↔BackLeft pair keeps measuring travel) gate the `UpdateStateMachine` commit: the flip fires when the live yaw/pitch passes the pair's signed boundary trigger, with `HysteresisFramesRemaining` kept ONLY as the same-frame jitter backstop (a raw flip that retracts within a frame never accumulates) — a fast drag commits at the same ABSOLUTE angle as a slow turn. The commit key coincides exactly with the crossfade α = 0.5 key (same constant), so at the instant the view flips the incoming card is already half-swapped in. The B.2 sign rule was extracted into a shared member `GetTransitionDirectionSign(From, To, bPitchBoundary)` used by BOTH the commit and the blend tick. `AngleHysteresisBuffer` (1.5°) keeps its angle-based hold while hovering inside the buffer; `HysteresisFrames` re-scoped to the jitter backstop. — `TestParameterSpaceTriggers` (43 pins: trigger points, forward/reverse crossing exactly at trigger, left-half mirror, no-fire-inside-±1.5°-band sweeps, monotonic past trigger, ±180 wrap pair, commit-key==crossfade-midpoint, sign normalization, speed independence, StateMachine band/retreat mirror) + re-pinned `TestEdgeCases` (23.0° hover in the band NEVER commits, 24.1° past the forward trigger commits, reversed 21.5°/21.0°) + harness `StateMachine`/`AnimHost` mirrors rewritten to the Schmitt+backstop semantics.
- **Authored-pose validation**: `FPOutlineIsValidClosedRing` (≥3 finite points in [0,1]², implicit last→first closure), `FPSchematicValidatePoseSet` (all 7 rings valid AND front-count-matched, back-change = fraction of ring points displaced >10% of canvas), and `FPSchematicValidateAllAuthoredPoses` (aggregate back-change must clear the measured 41% gate — live data: 168/189 = 88.9%) with a negative control (a back pose that is a copy of the front fails the gate; a count mismatch invalidates the set). — `TestPhase6PoseValidation`.
- **Up/down view scrub (Phase C)**: dedicated vertical pitch strip `SFacePitchStrip` mounted LEFT of the canvas (the yaw zone bar sits above the schematic canvas; the strip is built in `BuildPanelCanvas` beside the 450px canvas) — drag up = toward Top, down = toward Bottom, relative pixels fed through the pure `FPLayout::FPZoneScrubPitchAfterDrag` contract (full strip = 180°, clamped `[-90,90]`, NO wrap) driving `SetOrbitPitch` + `ActiveViewState` live (`DetermineStateFromAngles` parks at the Top/Bottom art states past their thresholds); commit snaps pitch to the canonical state center so Top parks at +90 / Bottom at −90; the widget schematic consumption (`RefreshSchematic`) now prefers the scrub yaw/pitch while either strip is dragging (`bScrubbing = bZoneScrubbing || bPitchScrubbing`). — `TestPhaseCUpDownScrub` + the Phase B vertical-parallax pins in `TestPhase2Orientation`.
- **360 rotation bar above the schematic + camera-orbit strip order (art-guide Req 4/5)**: the yaw zone diagram (the "360 rotation slider": yaw/pitch readout + 20px zone strip + `SZoneBoundaryOverlay` boundary drags + empty-space yaw scrub) moved OUT of the widget-top root row into the center column directly ABOVE the canvas — `BuildPanelCanvas` mounts `BuildPanelZoneDiagram` between the filter row and the canvas; manifest node `CN-ZoneDiagram` is a CENTER child (the old root-level `ZoneDiagram` row is gone; root rect re-pinned 1089×1054). The strip READS IN CAMERA-ORBIT ORDER: Left → 3/4L → Front → 3/4R → Right → BackR → Back → BackL, left-to-right with the right edge wrapping back to the left (BkL is the LAST segment; `RebuildZoneDiagram` emits the 8 zones in that order). The overlay's boundary lines + yaw cursor rebase via the new pure `FPLayout::FPZoneStripPixelForYaw` contract (left edge = −135 = the Left-profile zone start, `FPZoneStripRebaseDeg`); the relative-drag scrub (`FPZoneScrubYawAfterDrag`) is order-independent and unchanged. — `TestPhase1ZoneScrub` strip pins + the Req 4 row-order/above-canvas/root-removal checks in `TestPhaseHUIDesign`.
- **Phase 7 art swap (the swap IS the turn)**: real 2D art cards never deform, so the ONLY legitimate "turn" is a discrete per-view swap. `FPSchematicStateAtAngles` resolves the nearest state for any yaw/pitch (default-geometry mirror of `DetermineStateFromAngles`), `FPSchematicBracketStates` gives the two bracketing states + the smoothstep blend weight (the crossfade target; the "previous" art slot is the bracket partner, never the last state), `FPSchematicStatePoseOut`/`FPSchematicOutlineForState` give the EXACT pose per state (left half = mirror of right; authored silhouettes snap, features return the FROZEN front glyph or EMPTY when hidden), `FPSchematicLayerArtAlpha` is the per-state art-availability fade target (tag renders AND the state's slot has art), and `FPSchematicSwapModeFor` is the per-layer swap gate (structural gap OR outgoing-art-without-incoming-art ⇒ Swoosh). `FPOrientationOutline` now SNAPS to the nearest state — the Phase 2–5 morph/slide/fold/squash formulas are preserved for the pure pins but never run per-frame. **Back-half rule**: walk-behind states (BackRight/Back/BackLeft) hide EVERY non-silhouette card — the "near eye stays through the back" fallback is removed, the back read is the silhouette back poses alone. **Runtime wiring**: `UpdateStateMachine` sweeps when any base-preset layer leaves art behind (`FPSchematicSwapModeFor` aggregate over `FaceMaterialsByLayer`), and `RefreshLayerVisibilityForState` gates quads by `FPSchematicLayerArtAlpha(State, Tag, bHasArt)` — a layer with no incoming art is hidden at transition start ("fades before center"); the master material is opaque (MSM_UNLIT) so alpha 0 renders as hidden. **Widget parity**: `RefreshSchematic` resolves `FPSchematicStateAtAngles` for the scrub/active angles and passes per-part `FPSchematicLayerArtAlpha` to `SFaceSchematicLayer::SetPartAlpha` — glyphs the runtime hides render dimmed but stay clickable (the P1 one-map never loses glyphs). Top/Bottom are pose slots (bracketing along pitch at ±60, no vertical squash in the per-frame path). — `TestPhase7ArtSwap` + re-pinned `TestPhase3Visibility` (all-pairs-hidden walk-behind) + re-pinned `TestPhase2Orientation`/`TestAuthoredOrientation` snap pins.
- **Phase 8 (parallax + hard swap, the master blueprint turn)**: the phase-7 "snap to nearest pose with no motion" defect is replaced by REAL motion that never deforms art — `FPOrientationOutline` slides every vertex by the SAME dx/dy (`FPSchematicTagParallaxRate` + `FPSchematicParallaxSlidePeak`; velocity hierarchy +100% Nose+Bangs / +60% Eyes+Brows+Mouth+Cheeks / 0% Face Base anchor / −50% Ears / −100% BackHair) so a uniform monoline is preserved, and at the zone key (0/45/90/135/180/±90 pitch) the pose HARD-SWAPS to the next view's authored pose via `FPSchematicParallaxRamp` (0 at a pose key, 1 at the next) — no vertex blend/morph. `FPSchematicViewZone` re-baselines to HalfZoneWidth 45, pitch thresholds ±45, boundaries 45/90/135/180; `FPSchematicStateAtAngles` uses half-open bands (boundary belongs to the NEXT view; |yaw|>180 → Back, 180 = Back). `FPSchematicBracketStates` back-wraps with B3=135, exclusive at 135, inclusive at ±180. Top/Bottom return the authored pitch pose EXACTLY (no encroach/slide). Walk-behind read keeps AnchorCritical Ears as flat back-fuzz (`FPSchematicLayerVisibleInState`) while features hide, and BackHair promotes to layer 1 in state 4 (`FPSchematicLayerOrderInState`). **Geometry ratio pins (Part I)**: `FPSchematicFaceGeometry` measures the face (midline eyes at y≈0.44, 5-part width rule), `FPSchematicMeasureFaceGeometry` + `FPSchematicFaceGeometryPasses`. — `TestPhase8ParallaxSwap` (velocity table + part-to-tag aliasing, slide peaks, ramp key/next-key, rigid-translation no-morph pins, outgoing-peak/incoming-exact swap, Top/Bottom exact, BackHair promote, geometry passes) + re-pinned `TestPhase2Orientation`/`TestAuthoredOrientation`/`TestPhase3Visibility`/`TestPhase7ArtSwap` (ears persist walk-behind, back-hair layer promote, both eyes frozen at 3/4 key, hard-swap-at-90, zone-boundary 45/90/135/180, pitch bracket crosses at ±45).
- **Translation-only pins + velocity feed (master blueprint enforcement)**: per "art is never modified/transformed — you use parallax and swap", the runtime pin transforms are re-scoped to translation-only — `PinRotationFromYawDev`/`PinRotationFromViewAngles` ALWAYS return 0 and `PinScaleFromView` ALWAYS returns 1.0 (the pin still translates via `ProjectPinToUVInternal`; signatures preserved for compat), and `ComputeOffsetForState` now feeds the velocity hierarchy as the per-frame displacement authority for the 10 known base-preset tags (`FPSchematicTagHasParallaxRate` + `FPSchematicTagParallaxRate` replace the legacy DepthScale/invert composite for those tags; unknown/user tags keep the composite), mirrored by the new `FPYawRule::ComputeVelocityOffset`. — re-pinned `TestPrimaryLayerPin`/`TestPinRotation`/`TestNestedEffectiveTransform` to the identity contract (rotation 0 / scale 1.0; the effective transform never adds view-angle rotation) + added the `TestYawRule` velocity pins.
- **Placeholder art library (vector line art, all 17 parts × 13 views)**: `generate_art.py` (repo root) parses `FPSchematicAuthoredPoseTable()`/`DefaultPartSchematics()` from `FaceParallaxSchematic.h` and emits `Art/<Part>/<Part>_<State>_Y<Yaw>_P<Pitch>.svg` — 221 vector pieces (no raster), one folder per part. The SVGs use the `smooth_art.py` art-attractiveness engine: Catmull-Rom → Cubic Bezier curve smoothing (I.7 Curve Continuity), curvature-based sharp-corner detection (I.7 Shape Contrast — ~4 rounded : 1 sharp), per-part feature constructions (I.6: eye wedge+disconnected-lower-lash+iris-fill+highlights, mouth open-curve+gap, hair ribbon inner-boundary+gloss-fill, brow smooth-arch, nose angular-triangle), and solid-fill highlight/gloss patches (I.1 monoline + fill patches). View→ring resolution mirrors `FPSchematicStatePoseOut` + the `FPSchematicPairPartner` pair swap exactly (left-half states 5–7 resolve the partner's ring mirrored); Bottom (state 9) is excluded per art_guide Part V.4/VIII (no `Pn90` token — parallax carries the under-plane). Idempotent: `py generate_art.py` from the repo root. — `TestArtLibrary` (the 17×9 encode contract: slot/mirror/pair-swap identical to `FPSchematicStatePoseOut`, valid closed front-count-matched rings, left-half = exact mirror of the right-side partner, every visible piece equals `FPSchematicOutlineForState`, Bottom excluded + its ring still exists/valid).
- **Section 10 smooth-curve paint contract (smooth_art.py ported to C++)**: `FaceParallaxSchematic.h` section 10 adds `FPSchematicCurveCmd`/`FPSchematicArtChain`/`FPSchematicArtFace` + builders (`FPSchematicDetectSharpCorners`, `FPSchematicBuildSmoothChain` with the Python merged-first-cubic quirk AND the `M == 1` single-vertex-run straight-L case, `FPSchematicBuildOpenBezierChain`, `FPSchematicEllipseChain`, `FPSchematicArtFaceForRing`) — pure C++17, mirroring `smooth_art.py` VERBATIM so the widget draws the SAME art the `.svg` library was generated from, from the same ring data, with no file I/O. Chain model: `CovEdgeA/B`/`WrapCov` map a command to the full-ring edges it covers for the widget's per-edge occlusion dash (-1 = decorative accent, never dashed); `Tint` 0 = part stroke color / 1 = light `#d0d4da` highlight-gloss fill / 2 = stroke-colored `#16181d` flat fill (iris); `Order` 0 = fill (painted first), 1 = stroke; `Opacity` per patch (iris 1.0, hl1 0.85, hl2 0.6, head gloss 0.2, hair gloss 0.3). The character-preview painter `SFaceSchematicLayer::OnPaint` now renders each part's resolved ring through `FPSchematicArtFaceForRing` instead of straight polygon lines — strokes tessellated (cubics at 16 steps) to `FSlateDrawElement::MakeLines` with the occlusion dash at COMMAND granularity (`DrawSmoothChain`), closed ellipse fills as triangle-fan `FSlateDrawElement::MakeCustomVerts` fans (`DrawFillChain`), halo/flash routed through the contour chain; the ring stays the hit-test/filter/focus/hover surface (`SFaceHotspotLayer`, P1 one-map untouched). Fills keep the canonical art tints dimmed with part alpha; strokes keep the existing depth-class / edge-map / selection tinting. — `TestSVGPaintSmooth` (golden FRONT-ring geometry pins for eye/`Mouth`/`Head`/`Bangs`/`Nose`/`Teeth` lifted from the emitted golden paths, chain model + coverage-map invariants, decorative/accent chains never dash, right-eye mirror, the M==1 Teeth straight-L regression pin, and a 17-part × 9-view sanity sweep over `FPSchematicArtFaceForRing`).
- **Phase II contract work items (WI2–WI6, art_tech_guide)**: new pure schematic contracts — `FaceParallaxSchematic.h` Sections 13–17. **WI2** the per-sample directional Schmitt step `FPSchematicSchmittStep` (one neighbor per sample; hard commit at `FPSchematicSchmittTriggerAt` 1.5° via the canonical 12-pair `FPSchematicYawBoundaryForPair` table — the component's default edges {0.5,1,1.5,3,6,8,−8,−6,−3,−1.5,−1,−0.5}×HZW 22.5, so Front|NarrowR sits at 11.25, NarrowR|3QR at 22.5, 3QR|SliverR at 33.75 — NOT the state centers; the 22.5/67.5 sub-swaps fire at their own edges) + the III.6 local-delta-reset `FPSchematicThetaFiredRebase` (offset = Peak×[sin(θ)−sin(θ_fired)], zero exactly at the captured trigger, velocity-continuous). **WI3** camera proximity `FPProximityFactor` (clamped K/max(Z,Z_min); F=1.0 at ref 100, clamps 0.25/2.0 — never diverges at Z→0) + `FPSchematicProximitySwapRamp` (proximity-scaled completion: a close-up finishes the swap earlier over the same screen travel, long shot never widens) + `FPSchematicSeamMargin` (percentage × F_prox with an F-scaled floor) + `FPSchematicBakeRegionClamp` (a state-key bake region never straddles the seam; the 22.5/67.5 sub-keys collapse to pure slide — no bake). **WI4** anchor-critical read registration: `FPSchematicAnchorAngleForPart` (17 registrations — the 4 silhouettes ride the cranium origin, Neck Jaw −94°, Teeth −52°, Cheeks Cranium ±30/−35°), `FPSchematicAnchorProjectionAt` (XIV.1 master projection: Theta = θ₀+yaw, Phi = φ₀+pitch, one radius per domain — 1R cranium, 1.5R jaw, +Y up), `FPSchematicAnchorCriticalInReadBand` (every AnchorCritical part stays inside |Dx|,|Dy| ≤ 1R under ±45° pitch at all 7 sampled yaws). **WI5** pin lag `FPSchematicLagVelocity` (0.20 flicker fraction, clamped) + `FPSchematicChainDecay` (0.70 per link) + the S1 ease-back `FPPinLagCurve`. **WI6** `FPShapeContrastRatio`/`FPShapeContrastPasses` (XIII.2 ~4:1 rounded:sharp gate) + `FPSchematicShapeContrastForRing` — live canonical data reads 195 round vs 3 sharp (brow tips + hair tip), aggregate 65:1 passes. — `TestPhaseIISchmittStep`/`TestPhaseIIProximity`/`TestPhaseIIAnchorRead`/`TestPhaseIIPinLag`/`TestPhaseIIShapeContrast`.
- **Validation batch 2 (A.7 pins, A.8, A.10, B.12 — art_tech_guide XIII.6, section-10 Order-0 contract, Part VI)**: **A.7** per-state mask-read canonical pins — `TestPhaseA7MaskRead` resolves the canonical mask (visible Head+Bangs+Hair+BackHair+EarL+EarR via the product path) for ALL 14 states and pins: single filled component + sub-noise holes, 0.5% hole gate, mask validity, silhouette fills > half canvas, exact mirror-pair area equality (pairs {0,11},{2,9},{3,10},{4,8},{5,7}, ≤5e-5 — the P135 pair is only ~1.19e-5 apart by construction), slot-consistency (HoleCount equality + area tolerance), and authored-slot area re-pins (P0 0.641717 / P45 0.618281 / P90 0.602187 / P135 0.594261 / P180 0.730803 / PTop 0.583375 / PBottom 0.548176, ≤1e-5). **A.8** (XIII.6 deliberate-asymmetry counter) — `FaceParallaxSchematic.h` Section 18 adds `FPSchematicCowlickInRing` (v2: ANY vertex protruding ≥0.003 above both neighbors with NO mirror twin `(1−x,y)` in the ring — v1 min-y-only failed the P90 flat hem), `FPSchematicRingCentroidX`, `FPSchematicMouthOffCenter` (≥0.01; P45 cx 0.5218 vs mirror 0.4782, front/back 0.5000, hidden states 4/5/6/7/8/12), `FPSchematicAsymmetryCueCount`, `FPSchematicAsymmetryContinuity` — the ahoge tip lives at (0.470,0.005) in the 27-vertex front Bangs ring, mirror state exactly 0.53. **A.10** (Order-0 fill-chain contract) — `FPSchematicFillChainPasses`/`FPSchematicArtFacePasses` assert the section-10 rules (closed+bFill, no escaping endpoints, containment incl. control-point bulges, decorative `WrapCov == -1` never dashes, Order-0 never stroke); the canonical-data sweep exposed TWO real fill escapes fixed in BOTH engines: the Hair/Bangs crown gloss was centered at `HMinY − HH/2` (spilled 0.033 above the crown) → now `HY = HMinY + HH` (C++ `FaceParallaxSchematic.h` + `smooth_art.py` `_build_hair_paths`), and the Head sheen (fixed ellipse (0.43,0.15) r=0.12/0.06) escaped the Top/Bottom head rings → both engines gate it on the ring bbox containing [0.31..0.55]×[0.09..0.21] (front views unchanged). **B.12** (Part VI residual) — `FPSchematicResidualCorrection` (`E = P_art − P_math` corner identity, `FPSchematicBilinearResidual` corner-exact to 1e-12, `FPSchematicResidualInTopWedge` single fixed offset, 60°==90°). — `TestPhaseA8Asymmetry`/`TestPhaseA10FillChains`/`TestPhaseB12Residual` + re-pinned `TestPhaseA7MaskRead` (2584/2584 total).
- **E-batch review fixes (art-library review items, all landed)**: **E5** Nose is its own encode token (sub-row split out of the Mouth row in the art grid); **E9** independently authored pitch-corner cells (art_guide XI.3/XVI.3 — the P45/Pn45 corners are per-part authored transforms, never duplicates of the yaw cells; `generate_art.py` self-checks non-duplication); **E10** walk-behind empties (art_guide XII.4 / Part IV Zone 5 — every BridgeSafe feature's Back3Q/Back cells ship as `EMPTY CELL` placeholder SVGs while silhouettes + Ears keep their back art); **E11** the three silhouette geometry gates, now enforced by `Tests\validator_silhouette.py` (pure Python, parses `FPSchematicAuthoredPoseTable`, wired into `run_tests.ps1` after the syntax validator; `--path` arg, `-NoPython` skips it): **gate 1** hair-ribbon separation at 3Q — the authored Hair table got crown bulges (P45 far-side crown band indices 8–11 outward +0.05→+0.03, P135 near-side right crown indices 12–19 +0.05→+0.03; the symmetric bulge rows 26–30/21–29 are deliberately untouched because the left-half mirror maps the bulged row onto them — adding both would double the bulge), measured post-fix P45 min far gap 0.0353 at Y~0.15 (gate ≥ 0.03) / P135 min near gap 0.0499 (gate ≥ 0.02); **gate 2** ahoge at every yaw (`FPSchematicCowlickInRing` v2 over all 7 Bangs rings); **gate 3** canthus preservation through the foreshortened eye cards — `FPSchematicScaleRingAboutCanthus` in `FaceParallaxSchematic.h` (anisotropic scale about the ring centroid IN THE RING'S OWN CANTHUS-CHORD FRAME — indices 0→9 both eyes; axis-aligned Y squash would steepen the tareme chord from −15° to −40°/−49°, the canthus-aligned scale preserves the chord angle EXACTLY while the card foreshortens; mirror-invariant; falls back to centroid scale for <10-point/degenerate rings) with the Python twin `scale_ring_about_canthus` in `generate_art.py`; the validator asserts chord-angle preservation to 1e-6 deg on all variant cards, the sliver width pin 0.025812242613, and a sensitivity negative control (axis-aligned sliver reads −40.316°, gate fails under 5° divergence). Variant factors unchanged: Narrow (0.85,0.95), Sliver (0.30,0.95), 3Q (0.88,0.95), roles via `FPSchematicIsFarSide`, applied at states 1/11 and 3/9. — re-pinned `TestPhaseA7MaskRead` slot areas `{0.641717, 0.631974, 0.602187, 0.605567, 0.730803, 0.583375, 0.548176}` (P45/P135 carry the E11 bulges) + the sliver width pin 0.025812242613 (2661/2661 total).
- **Renames are coordinated**: any future rename must touch manifest id + widget tab label + `TestPhaseHUIDesign`/`TestPhaseIUITesting` page-name arrays + README prose in the same change.
- **Stub host module**: `SAMPLES\MyProject\Source\MyProject\` — `IMPLEMENT_PRIMARY_GAME_MODULE(FDefaultModuleImpl, MyProject, "MyProject")`, targets `MyProject.Target.cs`/`MyProjectEditor.Target.cs` (`BuildSettingsVersion.V7`, `Unreal5_8`). All functionality lives in the plugin.
- **Deployment**: `deploy.py` (repo root) is THE deployment script — the one and only mechanism that creates every binary asset (master material, layer MIs, `DA_FaceParallaxPreset`, `BP_FaceParallaxCharacter`, `WBP_FaceParallaxEditor`, render target, preview actor). It runs inside the editor (`py "G:\tailedstories\paralax\deploy.py"`). If the FaceParallax plugin is missing from the project (fresh project / lost `Plugins\` folder), deploy.py self-installs it first: uplugin descriptor + module `Build.cs` rules (mirroring `run_tests.ps1`'s scaffold templates — keep in sync) + canonical sources from the repo root into `<Project>\Plugins\FaceParallax`, and enables the plugin in the `.uproject`; it then reports that a build (`Tests\run_tests.ps1 -IncludeUEBuild`) and editor restart are required. The install is idempotent and never overwrites existing files. There is NO C++ deploy pipeline in the subsystem — never re-add one, never port deploy.py's functionality to C++.

## Rules

1. **`EFaceAngleState` lives in `FaceParallaxTypes.h`** — include that header, never redeclare.
2. **Module API macro** is `FACEPARALLAX_API` — required on every runtime `UCLASS`/`USTRUCT`/`UENUM` the editor module references across the DLL boundary.
3. **Never break the Python syntax validator** — all `.h`/`.cpp` files must parse cleanly.
4. **Never break the C++ math tests** — component logic must match test expectations.
5. **Each plugin module needs `IMPLEMENT_MODULE`** in one of its cpp files (`FaceParallax\Private\FaceParallaxModule.cpp` for runtime, `FaceParallaxEditorSubsystem.cpp` for editor). Without it, the DLL loads but never registers — "could not be initialized successfully after it was loaded", or a "Trying to recreate changed class" fatal.
6. **Plugin dependencies must be declared in `FaceParallax.uplugin`** (`"Plugins": [...]`) as well as in `Build.cs` — missing entries produce UBT warnings and can break module loading.
7. **Keep `README.md` in sync** with API changes.
8. **Prefer editing existing files** — no new files unless necessary.
9. **Widget code uses direct member pointers** (`SliderOrbitYaw`, `PreviewImageWidget`) — no string-based dispatch.
10. **UE5.8 compiler**: `H:\unreal\UE_5.8\Engine\Build\BatchFiles\Build.bat` (run_tests.ps1 finds it via PATH).
11. **NEVER delete or remove anything** — no file, script, asset, `.uasset`, `.py`, `.md`, `.bat`, or any other artifact in this repository (root, `Tests\`, or `SAMPLES\`) may be deleted, renamed, or moved without the user's explicit written approval in the current turn. This includes any "cleanup", "removing duplicates", or "deleting stale assets". `deploy.py` and all deployment scripts are permanent — never delete them, never "port" their functionality elsewhere.
12. **`SAMPLES\` is read-only** — it contains ONLY the sample project (`SAMPLES\MyProject\`). Never save, copy, or generate any codebase file inside `SAMPLES\` (no README copies, no helper scripts, no docs). The only writer is `Tests\run_tests.ps1` syncing the 20 root sources into the plugin `Source\` dirs before a UE build. All codebase deliverables live at the root or in `Tests\`.
13. **No trash files** — never create scratch, backup, or duplicate files (e.g. `fixslots.ps1`, `deploy_legacy_backup.py`, stray README copies). If scratch already exists, remove it only with explicit user approval, then run the full suite.
14. **Art guides are law** — art should adhere to all principles in art_tech_guide and art_guide
15. **Never delete to fix** — a red test, a failing build, or a broken asset is fixed by editing code, never by deleting the thing that fails.
16. **Never claim results without running them** — "tests pass" may only be stated after `.\Tests\run_tests.ps1 -IncludeUEBuild` ran green in this session; report real numbers from real runs.

## Verify

**After EVERY change — C++, Python, docs, or any user-approved file deletion — run the full suite before finishing:**

1. `.\Tests\run_tests.ps1 -IncludeUEBuild` — syncs SAMPLES from root, runs the Python syntax validator, compiles and runs the C++ math tests (all must pass), and builds both plugin modules with UBT (must pass; checks both DLLs).
2. If the editor subsystem/widget changed: run the headless probe and check `SAMPLES\MyProject\Saved\Logs\MyProject.log` for the `[FaceParallax]` markers (`EditorSubsystem initialized - DOCKED-TAB BUILD v3 (marker 0xV3)`, `==> Deploying FaceParallax editor assets (deploy.py)`, `[VERIFY] Deployment complete - all assets present.`). Note: the `LayoutSpec self-check` log (Phase H) only fires when `RebuildWidget` runs in an interactive session (headless probes skip Slate) — its logic is covered by `TestPhaseHUIDesign` in the math tests.
3. If the editor is open (Live Coding active), the UE build will fail — close it and re-run the suite before declaring success.
